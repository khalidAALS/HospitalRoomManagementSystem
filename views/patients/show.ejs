<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Patient Details</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
  <%- include("../partials/navbar") %>

  <div class="container">
    <h1>Patient Details</h1> <!-- all listed patient details as saved within the system dispayed as below-->

    <p><strong>Name:</strong> <%= patient.name %></p>
    <p><strong>Age:</strong> <%= patient.age %></p>
    <p><strong>Condition:</strong> <%= patient.condition %></p>
    <p><strong>Symptoms:</strong> <%= patient.symptoms.join(", ") %></p>
    <p><strong>Infection Risk:</strong> <%= patient.infectionRisk ? "Yes" : "No" %></p>
    <p><strong>Isolated:</strong> <%= patient.isIsolated ? "Yes" : "No" %></p>
    <p><strong>Room:</strong> <%= patient.room || "Not Assigned" %></p>

    <hr>

    <a href="/patients/<%= patient._id %>/edit">✏️ Edit Patient</a> |
    <a href="/patients">🔙 Back to Patient List</a>

    <form action="/patients/<%= patient._id %>?_method=DELETE" method="POST" style="margin-top: 20px;"> <!-- deletes patient-->
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <button type="submit" onclick="return confirm('Are you sure you want to delete this patient?');"> <!-- delete confirmation-->
        🗑️ Delete Patient
      </button>
    </form>
  </div>
  <%- include("../partials/footer") %>

</body>
</html>
